import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CarouselScreen = () => {
  return (
    <View>
      <Text>CarouselScreen</Text>
    </View>
  );
};

export default React.memo(CarouselScreen);

const styles = StyleSheet.create({});
