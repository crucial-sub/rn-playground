import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ListSwipeScreen = () => {
  return (
    <View>
      <Text>ListSwipeScreen</Text>
    </View>
  );
};

export default React.memo(ListSwipeScreen);

const styles = StyleSheet.create({});
